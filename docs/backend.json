{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the BookNGlow platform, which can be either a customer or a salon owner.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "email": {
          "type": "string",
          "description": "The user's email address, used for authentication and contact.",
          "format": "email"
        },
        "role": {
          "type": "string",
          "description": "The role of the user within the platform, either 'customer' or 'salon'.",
          "enum": ["customer", "salon"]
        },
        "fullName": {
          "type": "string",
          "description": "The full name of the customer, only applicable if the user's role is 'customer'."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the user account was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the user account was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "email",
        "role",
        "createdAt"
      ]
    },
    "Salon": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Salon",
      "type": "object",
      "description": "Represents a salon registered on the BookNGlow platform, managed by a salon owner.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Salon entity."
        },
        "ownerId": {
          "type": "string",
          "description": "Reference to the User who owns and manages this salon."
        },
        "name": {
          "type": "string",
          "description": "The official name of the salon."
        },
        "localityId": {
          "type": "string",
          "description": "Reference to the Locality where the salon is physically located."
        },
        "contactEmail": {
          "type": "string",
          "description": "The primary contact email address for the salon.",
          "format": "email"
        },
        "categoryTier": {
          "type": "string",
          "description": "The category or tier level of the salon (e.g., Standard, Premium, Elite)."
        },
        "averagePriceRange": {
          "type": "number",
          "description": "An indicator of the average price range for services offered by the salon."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the salon record was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the salon record was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "ownerId",
        "name",
        "localityId",
        "contactEmail",
        "categoryTier",
        "averagePriceRange",
        "createdAt"
      ]
    },
    "Booking": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Booking",
      "type": "object",
      "description": "Represents a service booking made by a customer at a specific salon.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Booking entity."
        },
        "customerId": {
          "type": "string",
          "description": "UID of the user who made the booking."
        },
        "salonId": {
          "type": "string",
          "description": "ID of the salon where the service is booked."
        },
        "salonName": {
          "type": "string",
          "description": "Name of the salon at the time of booking."
        },
        "date": {
          "type": "string",
          "description": "The date of the appointment in YYYY-MM-DD format."
        },
        "time": {
          "type": "string",
          "description": "The time of the appointment (e.g., '10:00 AM')."
        },
        "status": {
          "type": "string",
          "description": "The status of the booking (e.g., 'confirmed', 'cancelled')."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp of when the booking was created.",
          "format": "date-time"
        }
      },
      "required": [
        "customerId",
        "salonId",
        "date",
        "time",
        "status",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "google.com"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores individual user profiles.",
          "params": [
            {
              "name": "userId",
              "description": "The unique Firebase Authentication UID of the user."
            }
          ]
        }
      },
      {
        "path": "/salons/{salonId}",
        "definition": {
          "entityName": "Salon",
          "schema": {
            "$ref": "#/backend/entities/Salon"
          },
          "description": "Stores public profiles and details for each salon.",
          "params": [
            {
              "name": "salonId",
              "description": "The unique identifier for the salon."
            }
          ]
        }
      },
      {
        "path": "/bookings/{bookingId}",
        "definition": {
          "entityName": "Booking",
          "schema": {
            "$ref": "#/backend/entities/Booking"
          },
          "description": "Stores service bookings. Accessible for availability checks and user history.",
          "params": [
            {
              "name": "bookingId",
              "description": "The unique identifier for a booking."
            }
          ]
        }
      }
    ],
    "reasoning": "Standard collection-based structure for users, salons, and bookings. Bookings are maintained in a global collection to facilitate cross-salon availability queries and centralized user history management."
  }
}